# Database Configuration
# Copy this file to .env.local and update with your database credentials

DB_HOST=localhost
DB_PORT=5432
DB_NAME=shopperdb
DB_USER=postgres
DB_PASSWORD=your_password_here

# Example for local development:
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=shopperdb
# DB_USER=postgres
# DB_PASSWORD=mypassword

# Example for production (Vercel, Railway, etc.):
# DB_HOST=your-db-host.com
# DB_PORT=5432
# DB_NAME=your_db_name
# DB_USER=your_db_user
# DB_PASSWORD=your_secure_password

# JWT
#JWT_SECRET=replace_with_a_long_random_string
#JWT_EXPIRES_IN=7d        # examples: 15m, 1h, 7d
#JWT_COOKIE_NAME=auth_token

# Cookie security (optional overrides)
#COOKIE_SECURE=true       # true in production (https). For local dev you can set false
#COOKIE_SAMESITE=lax      # lax is a balanced default; options: strict, lax, none
#COOKIE_MAX_AGE=604800    # 7 days in seconds; keep in sync with JWT_EXPIRES_IN